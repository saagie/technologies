version: v2
type: CONNECTION_TYPE
id: gcp
label: Google Cloud Connection

parameters:
  - type: TEXT
    id: jsonKey
    label: JSON key
    mandatory: true
    comment: "Full JSON document"

actions:
  checkConnection:
    script: ../../scripts/gcp/dist/check-connection.js
    function: checkConnection
