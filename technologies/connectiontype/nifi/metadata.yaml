version: v2
type: CONNECTION_TYPE
id: nifi
label: NiFi Connection

parameters:
  - type: TEXT
    id: url
    label: Root endpoint URL (nifi-api will be added)
    mandatory: true
    comment: e.g. use http://nifi.company.com:8080
