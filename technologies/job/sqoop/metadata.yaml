version: "v1"
type: JOB
id: sqoop
label: SQOOP
available: true
icon: sqoop
contexts:
  - id: 1.4.6
    label: 1.4.6
    available: true
    recommended: true
    trustLevel: stable
    job:
      features:
        - type: COMMAND_LINE
          label: Command line
          mandatory: true
          comment: Sqoop
          defaultValue: "# You can customize the user on HDFS by setting HADOOP_USER_NAME variable\n You can customize Sqoop libs directory by setting SQOOP_CUSTOM_HOME variable\ndriver=xxx \nhost=\"x.x.x.x\"\nport=xxx \nusername=\"xxx\" \npassword=\"xxxx\" \ndatabase=\"xxxx\" \ntable=\"xxxx\" \nhdfsdest=hdfs:///tmp/sqoop_import \n\nsqoop import --connect jdbc:$driver://$host:$port/$database --username $username --password $password --as-textfile -m 1 --target-dir $hdfsdest --table \"$table\""
        - type: ARTIFACT
          label: Package
          mandatory: false
          comment: Any file necessary for the process, can be a zip file containing shell script, jar files, data etc.
        - type: SCHEDULER
          label: Scheduled
          mandatory: true
    dockerInfo:
      image: saagie/sqoop
      baseTag: 1.4.6
      version: 1.4.6-1.83.0_release-202103